将事件与其对应的处理逻辑绑定到一起，具备高度的解耦性与可扩展性，同时通过配置文件动态启用不同的事件

# 🚩 事件系统设计思路
✅ 目标

✔️ 每个 Event 类自带处理函数,默认包括trigger和recover

✔️ 支持事件动态加载（基于配置文件）

✔️ 与主环境解耦

✔️ 支持扩展新事件，无需改动核心代码

事件队列首先会在初始化时插入一些默认事件,
后续也可以通过回调等机制插入更多的其他事件。

当前event_queue会在每个env_step末期自动调用,
弹出小于当前环境时间的事件,并且瞬时执行。




