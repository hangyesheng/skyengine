当前任务包括：
1.Trainer的编写 (long term)。
2.多种指标的计算,在component callback中实现。
3.可以实现一些静态策略以进行agent的切换。
要点：规定静态策略的输入和输出，我认为输入可以多变，但是输出应该是确定了的。

当前支持component回调，需要实现一下indicator编写的范式，可以将其理解为物理环境需要的一些指标，会记录在cache中。
当前能想到的一些实现包括:
    小车的运输次数,job完成的各个时延,单个物件从开始到结束的时延,machine完成操作的工件数量