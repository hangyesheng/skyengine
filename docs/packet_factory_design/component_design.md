

AGV和Machine的运行都需要final_time,这个值是本次决策告知的执行时间
当前step_time固定为1,也就是说这个final_time是告知这两个组件到达什么时刻需要停止了。

对环境整体进行比较细粒度的定时管理看起来是不太可能的，因为当前环境的执行是固定按照step_time的
间隔来的。但是在AGV内部和Machine内部进行定时或者check都是可行的。

另外为了收集job本身的指标,也需要在job中添加回调函数。

